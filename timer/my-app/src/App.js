import React from 'react';
import Timer from './Timer';  // Adjust the path based on where the Timer component is stored

function App() {
  return (
    <div>
      <Timer />
    </div>
  );
}

export default App;
